# Required on Vercel — set these in your project's Environment Variables
AUTH_SECRET="generate-with: openssl rand -base64 32"
DATABASE_URL="file:./prisma/dev.db"

# Optional (NextAuth v4 compat fallback, not needed on Vercel)
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=
